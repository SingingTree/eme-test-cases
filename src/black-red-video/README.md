Black red video generated from files here using ffmpeg via:
```
ffmpeg -f concat -i input.txt -framerate 60 black-red.mp4
```
or
```
ffmpeg -f concat -i input.txt -framerate 60 black-red.webm
````
or similar.